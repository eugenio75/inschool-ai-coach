// Re-export types for backward compatibility
// Translations are now in src/locales/it.json and src/locales/en.json
// Managed by react-i18next — see src/lib/i18nConfig.ts

export type Lang = "it" | "en";

// TranslationKey is kept as string for compatibility
export type TranslationKey = string;
